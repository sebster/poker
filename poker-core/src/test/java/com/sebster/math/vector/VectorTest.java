package com.sebster.math.vector;

import org.junit.Ignore;
import org.junit.Test;

public class VectorTest {

	@Test
	@Ignore
	public void testToString() {
		// final Vector<Rational> v1 = new
		// ImmutableArrayVector<Rational>(RationalVectorSpace.ofDimension(3),
		// Rational.ONE, Rational.ZERO, Rational.ONE);
		// Assert.assertEquals("<1, 0, 1>", v1.toString());
		//
		// final Vector<Rational> v2 = new
		// ZeroVector<Rational>(RationalVectorSpace.ofDimension(2));
		// Assert.assertEquals("<0, 0>", v2.toString());
		//
		// final Vector<Rational> v3 = new
		// ZeroVector<Rational>(RationalVectorSpace.ofDimension(0));
		// Assert.assertEquals("<>", v3.toString());
	}

}
